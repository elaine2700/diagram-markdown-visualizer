{
    "name": "markdown-to-diagram",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "prepare": "npm run build",
        "build": "npm run build -w @markdown-to-diagram/core && npm run build -w @markdown-to-diagram/svelte",
        "build:core": "npm run build -w @markdown-to-diagram/core",
        "build:svelte": "npm run build -w @markdown-to-diagram/core && npm run build -w @markdown-to-diagram/svelte",
        "changeset": "changeset",
        "release": "changeset publish"
    },
    "devDependencies": {
        "@changesets/cli": "^3.0.2"
    },
    "optionalDependencies": {
        "@rollup/rollup-linux-x64-gnu": "*"
    }
}